import 'package:flutter/material.dart';

class KingAIScreen extends StatelessWidget {
  const KingAIScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("KING AI Builder")),
    );
  }
}
