import 'package:flutter/material.dart';

class DubbingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dubbing')),
      body: Center(child: Text('Dubbing UI placeholder', style: TextStyle(color: Colors.white))),
    );
  }
}